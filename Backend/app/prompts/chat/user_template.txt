Analyze the following conversation history and produce the next JSON response.

Known session context:
{session_context}

Conversation history:
{conversation_history}

- If tool results are already present in the conversation history, use them before deciding whether another tool is necessary.
- Do not ask again for information that is already available in the known session context unless you need clarification.
- If the user has already chosen or strongly implied an archetype, prefer moving from discovery into deck generation.
- If archetypes were already suggested earlier, avoid repeating the same suggestions unless there is a clear reason.
- If the user is asking for help choosing a deck, prefer suggesting a small number of practical archetype options before generating a final deck.
- If a last saved deck is available and the user asks for changes such as more consistency, lower budget, more aggression, easier piloting, or fewer bricks, use that deck as the starting point for refinement.
- If the known session context is already sufficient, prefer moving the conversation forward.